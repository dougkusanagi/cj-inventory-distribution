---
paths:
  - config/inertia.php
---

# Config

## Inertia DevTools opt-in
Keep Inertia DevTools disabled by default with INERTIA_DEVTOOLS_ENABLED. Enable it explicitly only when storage/inertia-devtools has correct write permissions; this prevents local development requests from failing because of stale web-server ownership.
